
#pragma once

// just in case
#define _USE_MATH_DEFINES
#include <cmath>

#include <cstdlib>
#include <vector>
#include <string>
#include <iostream>
#include <queue>

#define _RANDOM( a, b ) ( a + ( b - a ) * ( std::rand() / ( float )RAND_MAX ) )